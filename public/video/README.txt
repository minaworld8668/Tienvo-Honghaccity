Thả video nền hero (cô gái thư giãn bên hồ bơi xanh) vào đây:

  hero-pool.mp4    (bắt buộc — định dạng MP4/H.264, nên nén ~1080p, < 5–8 MB, 8–15 giây, loop mượt)
  hero-pool.webm   (tuỳ chọn — nhẹ hơn cho trình duyệt hỗ trợ)

Khi chưa có file, slide 1 sẽ hiển thị ảnh poster (clubhouse.jpg) thay thế.
Đổi tên/đường dẫn poster & video trong: src/data/content.ts → HERO.video / HERO.slides[0].poster
